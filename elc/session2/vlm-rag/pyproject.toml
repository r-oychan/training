[project]
name = "vlm-rag-demo"
version = "0.1.0"
description = "Local VLM RAG demo with Ollama + SQLite"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.25.0",
    "sqlite-vec>=0.1.6",
    "requests>=2.32.0",
    "rich>=13.9.0",
]

[tool.hatch.build.targets.wheel]
packages = []
